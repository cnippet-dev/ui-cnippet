---
title: Select
description: A common form component for choosing a predefined value in a dropdown menu. Built with Base UI and Tailwind CSS. Copy-paste ready.

links:
  doc: https://base-ui.com/react/components/select#api-reference
---

<ComponentPreview name="v-select-1" />

## Installation

<CodeTabs>

<TabsList defaultValue="cli">
  <TabsTab value="cli">CLI</TabsTab>
  <TabsTab value="manual">Manual</TabsTab>
</TabsList>
<TabsPanel value="cli">

```bash
npx shadcn@latest add @cnippet/select
```

</TabsPanel>

<TabsPanel value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @base-ui/react
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="select" title="components/ui/select.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Usage

```tsx
import {
  Select,
  SelectItem,
  SelectPopup,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
```

```tsx
const items = [
  { label: "Select framework", value: null },
  { label: "Next.js", value: "next" },
  { label: "Vite", value: "vite" },
  { label: "Astro", value: "astro" },
]

<Select items={items}>
  <SelectTrigger>
    <SelectValue />
  </SelectTrigger>
  <SelectPopup>
    {items.map((item) => (
      <SelectItem key={item.value} value={item}>
        {item.label}
      </SelectItem>
    ))}
  </SelectPopup>
  </Select>
```

## Anatomy

```tsx
<Select items={items} defaultValue="next"> {/* root, owns the value */}
  <SelectTrigger> {/* <button> with a chevron · data-slot="select-trigger" */}
    <SelectValue placeholder="Framework" /> {/* shows the selected label · data-slot="select-value" */}
  </SelectTrigger>
  <SelectPopup> {/* portal + positioner + list · data-slot="select-popup" */}
    <SelectGroup> {/* optional · data-slot="select-group" */}
      <SelectGroupLabel>Frameworks</SelectGroupLabel>
      <SelectItem value="next">Next.js</SelectItem> {/* data-slot="select-item" */}
    </SelectGroup>
  </SelectPopup>
</Select>
```

## Good to know

- Built on Base UI, not Radix. Pass `items` (an array of `{ label, value }` or a `{ value: label }` object) to `Select` so `SelectValue` can show the selected item's label instead of its raw value.
- `SelectContent` is an alias of `SelectPopup`. Prefer `SelectPopup`.
- For placeholder text, use `placeholder` on `SelectValue`, or add an item with `value: null` to `items`.
- `SelectItem value` can be a string or an object. With `{ value, label }` objects, the label is shown automatically.
- The state props are `value`, `defaultValue` and `onValueChange`. Add `multiple` to allow several selections; the value becomes an array.
- When opened with a mouse, the list covers the trigger with the selected item lined up on it, like a native select (if there's room). Set `alignItemWithTrigger={false}` on `SelectPopup` to open it below the trigger instead.
- Inside a `Field`, the select is connected to the label and validation automatically. Give it a `name` for form submissions.
- For a searchable list, use `Combobox`.

## API Reference

### Select

Root provider. Wraps Base UI `Select.Root`.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `T` | — | Controlled selected value |
| `defaultValue` | `T` | — | Initial selected value for uncontrolled usage |
| `onValueChange` | `(value: T) => void` | — | Callback when the selection changes |
| `items` | `T[]` | — | Array of selectable items for keyboard navigation and filtering |
| `disabled` | `boolean` | `false` | Disables the entire select |

### SelectTrigger

The button that opens the dropdown. Includes a `ChevronsUpDown` icon.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `"sm" \| "default" \| "lg"` | `"default"` | Visual size of the trigger |

### SelectValue

Displays the currently selected item's label inside the trigger.

### SelectPopup

The floating dropdown panel. Also exported as `SelectContent`.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `side` | `"top" \| "bottom" \| "left" \| "right"` | `"bottom"` | Side to open relative to the trigger |
| `sideOffset` | `number` | `4` | Gap in pixels from the trigger |
| `align` | `"start" \| "center" \| "end"` | `"start"` | Alignment relative to the trigger |
| `alignOffset` | `number` | `0` | Offset along the alignment axis |
| `alignItemWithTrigger` | `boolean` | `true` | Positions the selected item directly over the trigger |
| `anchor` | `Element \| VirtualElement` | — | Custom anchor element for positioning |

### SelectItem

Individual option inside the popup. Shows a checkmark when selected.

### SelectGroup / SelectGroupLabel

Groups related options with an optional label.

### SelectLabel

Accessible label for the select, rendered outside the popup.

### SelectSeparator

Horizontal divider between option groups.

### SelectButton

Alternative trigger that renders a plain `<button>` instead of `SelectTrigger`, useful for custom trigger layouts.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `"sm" \| "default" \| "lg"` | — | Visual size |
| `render` | `React.ReactElement` | — | Renders a different element in place of `<button>` |

## Examples

For accessible labelling and validation, prefer using the `Field` component to wrap selects. See the related example: [Select field](/ui/forms/field#select-field).

### Sizes

Renders the `sm`, `default`, and `lg` size variants to show how the select trigger scales across layout contexts.

<ComponentPreview name="v-select-2" />

### With Groups

Organizes options into labeled `SelectGroup` sections for categorized lists such as timezones or regions.

<ComponentPreview name="v-select-3" />

### Multiple Selection

Allows selecting multiple values simultaneously, displaying them as a comma-separated list in the trigger.

<ComponentPreview name="v-select-4" />

### Options with Icon

Prepends an icon to each option in the dropdown for visual identification of the available choices.

<ComponentPreview name="v-select-5" />

### With Object Values

Demonstrates using complex object values (e.g. `{ id, label }`) rather than plain strings, letting you store structured data on selection.

<ComponentPreview name="v-select-6" />

### Form Integration

Wraps the select in `Form` and `Field` for required validation and a `FieldError` displayed on submit.

<ComponentPreview name="v-select-7" />

### With Disabled Options

Available plans are listed first; enterprise and custom tiers appear below a separator as disabled items with a "Contact us" label.

<ComponentPreview name="v-select-8" />

### Country Selector with Groups

Countries organized into `SelectGroup` regions (North America, Europe, Asia Pacific, Latin America) with emoji flags and a scrollable popup.

<ComponentPreview name="v-select-9" />

### Controlled with External Display

A theme selector whose selected value drives a description card rendered below the trigger — shows how to wire `onValueChange` to external UI.

<ComponentPreview name="v-select-10" />

### Role Selector with Disabled Options

Active role options are listed first; unavailable roles (Guest, Contractor) appear below a separator as disabled items with a "coming soon" label.

<ComponentPreview name="v-select-11" />

### Region Selector with Groups

Options organized into `SelectGroup` regions (Americas, Europe, Asia Pacific) with `SelectGroupLabel` headings for each continent.

<ComponentPreview name="v-select-12" />

### Priority Badge Display

A priority selector whose selected value drives a `Badge` rendered below the trigger — shows how to wire `onValueChange` to external UI.

<ComponentPreview name="v-select-13" />

### Items with Description

Each dropdown option includes a secondary description line below its label — useful for environment selectors, plan tiers, or deployment targets.

<ComponentPreview name="v-select-14" />

### Sort Order with Icons

A compact sort select where each option is prefixed with a `lucide-react` icon representing the sort direction or criteria.

<ComponentPreview name="v-select-15" />
